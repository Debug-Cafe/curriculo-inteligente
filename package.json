{
  "name": "curriculo-inteligente-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd curriculo-inteligente && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd curriculo-inteligente && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../curriculo-inteligente && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}